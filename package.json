{
  "name": "evm-payment-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run quality && npm run typecheck && npm test && npm run deploy:dry-run",
    "check:staged": "biome check --staged",
    "deploy": "node scripts/deploy.mjs",
    "deploy:demo": "wrangler deploy --config wrangler.demo.jsonc --secrets-file .demo.secrets",
    "deploy:dry-run": "wrangler deploy --dry-run --config wrangler.api.jsonc --secrets-file .api.secrets.example && wrangler deploy --dry-run --config wrangler.sweeper.jsonc --secrets-file .sweeper.secrets.example && wrangler deploy --dry-run --config wrangler.demo.jsonc --secrets-file .demo.secrets.example",
    "dev": "wrangler dev --config wrangler.api.jsonc",
    "dev:demo": "wrangler dev --config wrangler.demo.jsonc --env-file .demo.secrets",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "prepare": "husky",
    "quality": "biome check .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@turnkey/api-key-stamper": "0.6.11",
    "uqr": "0.1.3",
    "viem": "2.55.16"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8",
    "@cloudflare/vitest-pool-workers": "0.21.3",
    "@cloudflare/workers-types": "5.20260814.1",
    "husky": "9.1.7",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.123.0"
  },
  "engines": {
    "node": ">=22"
  },
  "cloudflare": {
    "bindings": {
      "PAYMENT_API_KEY": {
        "description": "Random server-to-server API key with at least 24 characters."
      },
      "TURNKEY_ORGANIZATION_ID": {
        "description": "Turnkey organization that owns the deposit wallet."
      },
      "TURNKEY_WALLET_ID": {
        "description": "Turnkey HD wallet used to allocate per-intent deposit addresses."
      },
      "TURNKEY_API_PUBLIC_KEY": {
        "description": "Public half of the address-allocator-only Turnkey API credential."
      },
      "TURNKEY_API_PRIVATE_KEY": {
        "description": "Private half of the address-allocator-only Turnkey API credential."
      },
      "NETWORKS_JSON": {
        "description": "JSON array of enabled EVM networks; see .dev.vars.example."
      },
      "PAYMENT_WEBHOOK_URL": {
        "description": "Absolute HTTPS URL that receives payment events."
      },
      "PAYMENT_WEBHOOK_SECRET": {
        "description": "Random HMAC signing key with at least 24 characters."
      }
    }
  }
}
